# ------------------------------------------------------------
# NookMesh Subscriptions Service
# Daily maintenance tasks
# ------------------------------------------------------------

5 0 * * * cd /nookmesh && /bin/bash auth/generate.sh >> /proc/1/fd/1 2>&1